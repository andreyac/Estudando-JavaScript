let computador = {
    mem : 16,
    ssd : 512,
    gpu : "5500xt",
    proc : "Ryzen 5"
}

let {mem : memoria, ssd : arma, gpu : placaV, proc : processador} = computador;

console.log(memoria, arma, placaV, processador);